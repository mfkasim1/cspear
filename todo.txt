shape
array:
  * op:
    * maximum
    * minimum
  * reduce:
    * prod
  * matmul
  * strided views
  * indexed views
  * transpose views
  * casting

test:
  * contiguous view array with int

SIMD:
  * vector int

optimize:
* optimize filter view (not constructing the indices until it is necessary)
* save a contiguous copy for non-contiguous view
* SIMD!
