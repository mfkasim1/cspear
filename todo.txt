shape
array:
  * interp
  * histogram
  * reduce with given axis/axes

  * reduce (mean, std)
  * contiguous slicing
  * matmul
  * non-contiguous slicing
  * casting

optimize:
* optimize filter view (not constructing the indices until it is necessary)
